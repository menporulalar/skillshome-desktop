# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# per-developer Claude Code approvals (shared config lives in settings.json)
.claude/settings.local.json

# Feature #28 — Desktop auto-update signing keys (NEVER commit private keys)
src-tauri/.tauri-keys
src-tauri/.tauri-keys.pub
src-tauri/.tauri-keys.*
